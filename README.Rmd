---
output: github_document
---

<!-- README.md is generated from README.Rmd. Please edit that file -->

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.path = "man/figures/README-",
  out.width = "100%"
)
```
# Statistical Hypothesis Testing in R

`SHT` was designed to provide a casket of tools for hypothesis testing procedures ranging from classical to modern techniques.

## Installation

Currently, `SHT` can only be downloaded via github using following commands,
```r
## install.packages("devtools")
## library(devtools)
devtools::install_github("kisungyou/SHT")
```

## List of Available Tests
We categorized available functions by their object of interest for better navigation.

* Notations **$x$** and **$y$** refer to samples. 
* Authors are referred by last names. See the help page of each function for complete references. 
* **$k$-sample** means that the test is checking the _homogeneity_ across multiple samples.
* Function naming convention is {`type of test`.`test name`}, or {`type of test`.`year` `authors`}, where there are two or three authors, we took their initials as abbreviation or simply the last name of the first author otherwise.
* Since $k$-sample test is applicable for a special case of $k=2$, two functions - **`usek1d`** and **`useknd`** - are provided to extend capabilities of any $k$-sample tests in our package to be applicable for $2$-sample testing.

### 0. utilities 
| function name | description |
|---------------| ------------|
| `usek1d`      | apply $k$-sample test method for two univariate samples |
| `useknd`      | apply $k$-sample test method for two multivariate samples |

### 1. tests for univariate mean $\mu \in \mathbb{R}$
| function name | authors| description of $H_0$ |
|-------|------------------------|:----------------|
| `mean1.ttest` | Student (1908)                  | $\mu_x ~\{\leq, =, \geq\}~ \mu_0$ (1-sample) |
| `mean2.ttest` | Student (1908) & Welch (1947) | $\mu_x ~\{\leq, =, \geq\}~ \mu_y$ (2-sample) |
| `meank.anova` |               -                 | $\mu_1 = \cdots = \mu_k$ ($k$-sample) |

### 2. tests for multivariate mean $\mu \in \mathbb{R}^n$
| function name | authors | description of $H_0$|
|---------------|------------------------|:----------------|
| `mean1.1931Hotelling` | Hotelling (1931)       | $\mu_x = \mu_0$ (1-sample) |
| `mean1.1958Dempster`  | Dempster (1958, 1960)  | $\mu_x = \mu_0$ (1-sample) |
| `mean1.1996BS`       | Bai and Saranadasa (1996) | $\mu_x = \mu_0$ (1-sample) |
| `mean1.2008SD`| Srivastava and Du (2008)  | $\mu_x = \mu_0$ (1-sample) |
| `mean2.1931Hotelling` | Hotelling (1931)       | $\mu_x = \mu_y$ (2-sample) |
| `mean2.1958Dempster`  | Dempster (1958, 1960)  | $\mu_x = \mu_y$ (2-sample) |
| `mean2.1996BS`       | Bai and Saranadasa (1996) | $\mu_x = \mu_y$ (2-sample) |
| `mean2.2008SD`| Srivastava and Du (2008)  | $\mu_x = \mu_y$ (2-sample) |
| `mean2.2014CLX` | Cai, Liu, and Xia (2014) | $\mu_x = \mu_y$ (2-sample) |
| `mean2.mxPBF` | - | $\mu_x = \mu_y$ (2-sample) |

### 3. tests for variance $\sigma^2$
| function name | authors | description of $H_0$|
|---------------|------------------------|:----------------|
| `var1.chisq` | -        | $\sigma_x^2 ~\{\leq, =, \geq\}~ \sigma_0^2$ (1-sample) |
| `var2.F`     | -        | $\sigma_x^2 ~\{\leq, =, \geq\}~ \sigma_y^2$ (2-sample) |
| `vark.1937Bartlett` | Bartlett (1937) | $\sigma_1^2 = \cdots = \sigma_k^2$ ($k$-sample) |
| `vark.1960Levene`   | Levene (1960)   | $\sigma_1^2 = \cdots = \sigma_k^2$ ($k$-sample) |
| `vark.1974BF`    | Brown and Forsythe (1974) | $\sigma_1^2 = \cdots = \sigma_k^2$ ($k$-sample) |

### 4. tests for covariance $\Sigma$
| function name | authors | description of $H_0$|
|---------------|------------------------|:----------------|
| `cov1.mxPBF`  | - | $\Sigma_x = \Sigma_0$ (1-sample) |
| `cov2.2012LC`    | Li and Chen (2012) | $\Sigma_x = \Sigma_y$ (2-sample) |
| `cov2.2013CLX`    | Cai, Liu, and Xia (2013) | $\Sigma_x = \Sigma_y$ (2-sample) |
| `cov2.mxPBF`    | - | $\Sigma_x = \Sigma_y$ (2-sample) |
| `covk.2001Schott`    | Schott (2001) | $\Sigma_1 = \cdots = \Sigma_k$ ($k$-sample) |
| `covk.2007Schott`    | Schott (2007) | $\Sigma_1 = \cdots = \Sigma_k$ ($k$-sample) |

### 5. simultaneous tests for mean $\mu$ and covariance $\Sigma$
| function name | authors | description of $H_0$|
|---------------|------------------------|:----------------|
| `sim1.2017Liu`  | Liu, Liu, Zheng, and Shi (2017) | $\mu_x = \mu_0 \textrm{ and } \Sigma_x = \Sigma_0$ (1-sample) |

### 6. distribution tests of normality
| function name | authors | description of $H_0$|
|---------------|------------------------|:----------------|
| `norm.1965SW` | Shapiro and Wilk (1965) | $F_X = \textrm{Normal} \in \mathbb{R}^1$|
| `norm.1972SF` | Shapiro and Francia (1972) | $F_X = \textrm{Normal} \in \mathbb{R}^1$|
| `norm.1980JB` | Jarque and Bera (1980) | $F_X = \textrm{Normal} \in \mathbb{R}^1$|
| `norm.1996AJB`| Urzua (1996) | $F_X = \textrm{Normal} \in \mathbb{R}^1$|
| `norm.2008RJB` | Gel and Gastwirth (2008) | $F_X = \textrm{Normal} \in \mathbb{R}^1$|

### 7. distribution tests of uniformity